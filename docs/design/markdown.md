# Markdown 渲染

日期：2026-09-11。状态：已实施并通过当日模拟浏览器验收。消息安全约束见本文；项目文件预览与相对链接以 [工作区面板](workspace-panels.md) 为准。

## 目标与范围

Codex 回复支持 Markdown，在生成中、历史回放和重新进入会话时保持一致的排版。用户消息保留原文和换行；命令输出、工具 JSON、文件 diff 与受控图片附件使用现有组件。原生历史仍保存原文，不修改后端协议。

选用 Streamdown 2.6.0 与 @streamdown/code 1.1.1，复用其 GFM、未闭合语法处理与 Shiki 高亮。无需引入 AI SDK、shadcn 或自行编写 Markdown 解析器。参考：https://github.com/vercel/streamdown 。

## 展示与交互

- 正文：标题、段落、列表、引用、加粗、删除线、行内代码、代码块、表格、任务列表与链接。
- 用户输入及乐观消息保持纯文本；不改变发送、插话或失败恢复行为。
- 代码块显示语言与一键复制，生成中使用纯文本、完成后启用语法高亮。未知语言降级为纯文本。代码不强制换行，长行仅在代码块内部横向滚动。
- 表格有表头、边框和清晰间距，窄屏在自身容器内横向滚动。
- 回复提供“复制 Markdown”，复制原文而非 DOM 文本；按钮报告成功或失败。代码复制保持原缩进。
- 根据所属 turn 与当前 activeTurnId 判定正在生成的消息，历史消息用静态解析；实时更新已由 [增量同步](session-sync.md) 替代早期 200ms 全快照调度。
- 消息保持 memo 与稳定历史引用；共享惰性加载外壳隔离消息和项目文件的解析作用域。异步高亮造成高度变化时也遵守现有跟随开关，向上阅读不被拉回底部。

## 0.1.4：会话文档链接（2026-09-18）

- 修复原因：聊天默认只接受 HTTP/HTTPS，合法文档路径被清空后由加固插件显示 `[blocked]`；Windows 盘符及 `file:` 链接还会被前置清洗移除。
- 在清洗前，仅把当前绑定项目内的文档路径转换为已有的同源、受鉴权文件接口地址，再继续执行清洗和链接加固。复用文件面板的解析与预览，不新增任意文件读取接口。
- 支持项目相对路径、Windows/Linux 项目内绝对路径、对应 `file:` 地址，以及中文、空格和百分号编码。`:行号[:列号]` 与 `#L行号` 可打开所属文档；本版不定位具体行。项目外路径、网络文件地址、目录越界和危险协议继续拒绝，服务端仍检查真实路径、符号链接与敏感文件。
- 点击聊天文档链接展开右侧栏，切到“文件”并显示预览；手机打开右抽屉。重复点击可重新选中文档，缺失文件显示“文件已不可用”。历史及生成中的回复使用相同规则；文档内相对链接、外链新标签行为保持。
- 验证入口：`node --test tests/file-links.test.ts`；开发服务的 `/tests/document-links.html`（可加 `?streaming=1` 或 `?linux=1`）。桌面 1280×900、手机 390×844 覆盖打开、关闭重开、相对跳转、缺失文件及越界链接；原有 Markdown、面板、发送及历史性能回归一并验证。
- 实测：以上 7 个浏览器场景（文档静态/流式/Linux 路径、Markdown、面板、发送、历史性能）在两种尺寸共 14 组均通过，无应用控制台错误；旧面板夹具的 favicon.ico 404 与功能无关。Windows 全量测试 211 通过、5 跳过、0 失败；构建通过（沿用现有大分包提示）。
- 本地构建用于用户验收；0.1.4 暂不推送或发布，等待用户确认。

## 安全、性能与样式

不解析原始 HTML，不执行脚本或嵌入页面。外链仅允许 http/https，使用新标签及 noopener/noreferrer；危险协议不转成可执行链接，项目内本机路径通过下述文件预览转换。Markdown 图片不自动加载，现有鉴权附件显示保持原样。

保留库的清洗与加固处理，禁止通过自定义组件绕过链接过滤。高亮资源从本地构建产物加载；消息级错误边界覆盖顶层 Markdown 包及内部高亮子模块，加载或渲染失败时显示可选择复制的原文，不影响其他消息。首版不接 Mermaid、数学公式或代码运行功能。

Streamdown 与高亮组件通过动态 import 加载。高亮使用 JavaScript 正则引擎，无需放宽生产 CSP。生成中不执行高亮，避免为每个增量片段缓存 token；代码内容变化时重建 CodeBlock，隔离过期异步回调。当前高亮插件的缓存键仅取代码首尾和长度，可能将中部不同的代码误判为相同；统一核对 token 还原文本与本次原文，不匹配就显示纯文本，待上游修复缓存键后可移除这层保护。

Markdown 样式限定在消息正文，适配现有深色配色，覆盖当前全局 pre、code、white-space 和表单样式对排版的影响。任务复选框只读。避免全局样式改动影响工具面板。

## 历史实施步骤

以下保留首次实施顺序；原 MarkdownMessage 已在后续面板改造中提取为共享 Markdown 外壳，旧文件名只表示当时的改动位置。

1. package.json / package-lock.json：安装并锁定上述两个直接依赖。
2. tests/markdown.html：按已有浏览器自检页面模式建立回归，覆盖基础格式、流式切片、安全链接/HTML/图片、用户原文、复制、移动端溢出及高亮缓存一致性；tests/send-message.html 覆盖真实消息组件的生成状态、长历史和滚动。
3. src/web/MarkdownMessage.tsx：集中 Streamdown 配置、代码高亮、安全组件及复制反馈；使用 memo 稳定历史渲染。
4. src/web/Conversation.tsx / SessionView.tsx：仅替换 agentMessage 正文并传入当前生成状态，复用现有 item.id 与滚动跟随。
5. src/web/style.css：加载必要库样式并追加隔离的 Markdown 样式。
6. 运行 npm test 与 npm run build，在浏览器打开 /tests/markdown.html，并重跑 /tests/send-message.html 验证没有破坏即时发送/插话。
7. 核对生产构建、本地静态资源和安全 CSP 兼容；更新本文验收记录；将本次全部变更记录为一个中文提交。

## 验收标准

中文混排及所有首版格式可读；半截代码围栏、加粗和链接不导致崩溃；完成后的显示与历史一致。代码与回复复制结果和原文相符。长代码/宽表格不撑破手机页面。脚本、原始 HTML、危险 URL 和远程图片不能执行或自动请求。历史更新不造成持续重复解析，用户向上滚动后不会被自动拉回底部。

## 验收记录

- npm test：128 项，127 通过、0 失败、1 跳过（便携包集成测试需单独启用）。
- npm run build：TypeScript 检查和生产构建通过。Markdown 独立分包约 676 kB，gzip 209 kB；仍存在超过 500 kB 的构建提示，未隐藏警告。语言资源按需加载，首版暂不再拆分解析器。
- npm run dev 后打开 /tests/markdown.html 与 /tests/send-message.html：独立无头 Chrome 在 1100×900 和 390×844 下均显示 PASS，控制台无错误。已查看桌面和手机截图，表格内部滚动，页面无横向溢出。
- 格式回归验证实际 token 颜色、未知语言回退、代码及 Markdown 原文复制、剪贴板失败反馈、未闭合语法、危险链接和原始 HTML、远程图片占位、相同首尾及长度但中部不同代码的缓存冲突。
- 会话回归验证即时发送、插话、去重、草稿保护、失败恢复，并覆盖生成转完成、60 条历史更新、旧正文 DOM 保持及向上阅读时不抢滚动位置。
- 分别阻断 MarkdownMessage 与内部 highlighted-body 模块请求：该条回复回退原文，用户消息仍在，应用不崩溃。
- 生产包预览复用了发送回归的模拟 API，验证编译后的主包、Markdown 分包和代码高亮在限制性 CSP 下正常加载，无控制台错误；未调用真实会话接口。
- 浏览器插件因本机运行器连接超时不可用，采用仓库已安装的 Playwright 与独立无头 Chrome 验证。只读代码复核通过。
