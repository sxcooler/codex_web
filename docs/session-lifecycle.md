# 会话打开、刷新与释放

会话历史属于原生 Codex Thread；浏览器页面、实时连接和运行中的任务分别管理。流程参考 [Codex App 的项目与会话](https://learn.chatgpt.com/docs/projects)、[IDE 会话交互](https://learn.chatgpt.com/docs/codex/ide) 和 [App Server 协议](https://learn.chatgpt.com/docs/app-server)。协议中的 `thread/read` 只读取，`thread/resume` 恢复已有会话，`thread/unsubscribe` 释放订阅；归档是另一项操作。

## 用户流程

1. **打开网页或返回网页**：读取最近会话；打开的会话重新读取原生历史与状态。页面恢复、获得焦点和网络恢复也会触发同步，无需退出重登。
2. **刷新最近会话**：重读第一页并重置分页，使 VS Code 新建或归档的会话变化可见；失败时保留已有列表并提示错误。
3. **刷新当前会话**：重建实时连接并读取历史及状态；可再次点击来取消挂起的读取，单次读取最长等待 30 秒。读取期间暂停发送，避免使用旧状态提交。
4. **继续会话**：就绪或已释放时可输入并发送。已释放的 Thread 在发送时恢复，继续原有历史。单纯打开或刷新不会发送消息。
5. **中止**：仅中止当前 Web 管理的活动任务，等待原生终态后才能继续或释放。
6. **释放并关闭**：空闲时释放成功才返回新任务页，关闭当前实时订阅。有草稿时先确认是否丢弃；失败则留在原页并显示错误。已持久化会话仍可从最近会话打开。
7. **跨客户端接力**：若原生状态报告其他客户端正在运行，暂停发送与释放。对方完成并释放写入占用后刷新，再显式发送；不会强行抢占。

## 状态与按钮

“已连接”只表示实时通道已建立，不能据此判断任务是否完成。

| 会话状态 | 发送 | 中止 | 释放并关闭 |
| --- | --- | --- | --- |
| 就绪 / 已释放，可继续 | 可以 | 不可用 | 可以 |
| Web 运行中 / 等待审批或输入 | 等待本轮结束 | 可以 | 不可用 |
| 其他客户端运行中 | 不可用 | 不可用 | 不可用 |
| 状态待核实 / 同步失败 | 先刷新核对 | 已知有 Web 活动任务时可用 | 不可用 |

审批与输入通过对应交互卡片处理。提交结果未知时保留草稿，并要求用户核对历史后显式解锁重试；刷新和重连不会自动重发。

## 同步规则与边界

- 原生已完成、失败或中止的轮次覆盖旧的流式内容；漏收完成事件时清理对应活动标记和过期审批。
- 并发读取只允许最新发起的快照更新运行状态；读取期间发生的新操作或事件不会被旧快照解锁。状态修正通知其他页面，稳定读取不重复发通知。
- 分页期间可能完成任务：早先状态为 active、读到的轮次全部结束时，再确认一次原生状态。
- `notLoaded` 表示当前原生运行时未加载，不等于会话被删除。可恢复错误在确认无未完成工作后清除；下一次显式发送仍由原生 writer 检查决定是否能接力。
- 释放不归档、不删除历史。已经归档的会话需要在原客户端取消归档；Web 本次不新增归档管理。
- 当前 CLI 的空会话在首条消息前可能尚未落盘，释放或运行时退出后无法找回；页面保留该提示。未发送草稿不跨浏览器关闭保存。

## 回归检查

运行 `npm.cmd test` 与 `npm.cmd run build`。运行 `npm.cmd run dev` 后，在浏览器打开 `/tests/session-lifecycle.html` 与 `/tests/recent-sessions.html`；检查页使用模拟 API，不消耗模型额度、不改变真实会话。

后端覆盖 writer 冲突后恢复、释放后保留历史继续、漏失完成、分页完成窗口、并发读取及发送保护。页面检查覆盖恢复事件、挂起读取重试、释放后发送、失败留页与草稿保护。真实 VS Code 持有 writer 时仍会拒绝接力，不能把模拟检查视为所有客户端版本的兼容性保证。

## 第二阶段流程更新（源码）

离开会话、切换会话或打开新任务自动请求释放；活动任务和待审批保留，终态后继续释放。返回时取消尚未执行的释放，并等待已经在途的释放完成。浏览器按会话串行处理离开与返回，刷新、聚焦和重连均等待返回确认，避免快速往返造成错误释放。

手动“释放并关闭”只有在后端确认 handoffReady 后导航；未完成则留在当前页并显示原因。SSE 超过 45 秒没有有效事件时重连，重读历史不会重发用户消息。原生 willRetry 单独显示为 Codex 重试。

未发送的草稿、选项、附件及阅读位置在本次浏览器页面生命周期内保留；新建任务草稿同时保留项目/Clone上下文。完整刷新仅保证布局比例恢复，草稿未持久化到磁盘。已接受的附件通过私有数据库与文件目录持续保留。

本节是第二阶段实现说明；真实 VS Code 往返与手机通知尚未重新验收，见 [实施验收](../design-qa.md)。
