# Windows / Linux 便携包验证（2026-09-13）

范围：共享首次配置流程、Linux 原生打包、双语说明和发布归档。Windows x64 使用 PowerShell 7；Linux x64 使用 WSL2 Ubuntu 22.04、glibc 2.35、普通用户。固定运行时 Node 24.20.0。

| 检查 | Windows | Linux |
| --- | --- | --- |
| `npm test` | 174 通过，5 跳过 | 175 通过，4 跳过 |
| `npm run package:portable`（含类型检查、前端构建） | 通过 | 通过 |
| 包内后端加载和 sharp 原生图片处理 | 通过 | 通过 |
| 全新解压包清单、启动、密码登录、端口冲突 | 通过 | 通过（直接运行 `Start.sh`） |

产物检查使用含空格的目录，清除系统 Node 搜索路径，保留包内 Node 和原生 CLI，检查结束后停止测试服务；不调用模型。Linux 环境原有 npm CLI 包装脚本依赖 PATH 中的 Node，最初产物检查因此失败；改用同一 CLI 安装中的原生二进制后通过，不依赖宿主 Node。

首次安装流程的 10 项测试在两平台通过，覆盖默认手动安装、明确安装同意、失败重试、退出、不重复安装、旧路径修复和端口冲突；安装器调用使用替身，不重复联网安装或登录。Windows 跳过 4 项 Linux 专属检查和产物检查；Linux 跳过 2 项 Windows 专属检查、未开启的用户 systemd 集成检查和产物检查。产物检查已在两平台另行运行。无新的真实模型任务或沙箱验收；此前结果见 [Linux 验证](2026-09-13-linux.md)。

Linux 包用 tar.gz 保留执行权限，Windows 包用 ZIP；两者原生安装生产依赖，源码包只导出已跟踪且允许发布的文件。未验证其他 Linux 发行版、ARM64、musl/Alpine 或 macOS。Vite 仍有已有的大语言高亮资源块提示，不影响构建结果。
