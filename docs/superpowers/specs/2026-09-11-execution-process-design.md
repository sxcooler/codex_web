# 执行过程折叠设计

用户确认：先落文档再实施。将连续的思考与工具步骤合并成默认折叠的执行过程；降低嵌套展开的操作负担。

## 展示与分组

- 在每轮内按原始顺序归并连续思考、命令和工具步骤。用户消息、助手正文（含进度说明和最终回答）及其他非执行项打断分组，不跨轮、不重新排序。未知类型沿用原展示，避免误隐藏交互。
- 折叠摘要显示“执行中 / 执行过程 · N 个步骤 · 当前动作或结果”。运行中同样默认折叠，不随新输出或完成自动开合。
- 展开后使用紧凑步骤列表，没有内层大边框卡片。思考摘要、命令名称、状态直接显示；短思考直接显示，长思考及完整工具内容使用“查看详情”，命令使用“查看输出”。各项可同时展开。
- 命令运行中也只由用户打开。默认行仅显示命令摘要，展开输出时显示完整命令和原文输出，并复用现有大输出按需请求、缓存、错误重试。非零退出码仍如实显示，不直接等同于整个任务失败。

## 状态和可访问性

- 分组以轮次 ID 和首项 ID 为稳定标识；增量追加、加载更早历史、终态更新不重置展开状态。
- 外层关闭只隐藏已有内容，不卸载内层，重开后保留已打开步骤、输出缓存和内部滚动位置。首次外层展开才挂载步骤，避免折叠历史创建大量正文节点。
- 外层使用原生 details/summary；内层使用带 aria-expanded、aria-controls 的文字按钮和 hidden 内容区域。键盘可操作，父层收起后内层不进入焦点顺序。命令完整内容可以在详情中阅读，不依赖悬浮提示。
- 审批、待回答问题、会话错误、轮次失败提示留在外层之外；折叠不会阻挡必要操作。切换会话后重新进入默认折叠，本轮不增加持久化偏好。

## 实施和验收

修改 Conversation.tsx 完成分组与紧凑步骤，SessionView.tsx 按轮渲染，style.css 补充桌面与移动样式；不改变服务端接口和同步逻辑，不新增依赖。

真实浏览器模拟 API 检查：默认折叠含运行中命令、正文与插话顺序、审批/失败外显、短摘要无需再次点击、长内容文字按钮展开、多个输出同时打开、外层开合保留状态/缓存、增量和完成不改变开合、手机布局无横向溢出。回归现有分页/按需输出/同步/发送，运行生产构建与项目测试。最终本次改动提交为一个中文 commit。

## 实施结果

- 已完成连续步骤分组、默认折叠与紧凑展示。内层使用文字按钮，短思考直接显示，完整命令与输出可按需查看；未知类型保留原 details.tool 展示。
- Chrome 1400px / 390px 的生产资源验收通过：默认折叠包含运行步骤，正文/插话顺序不变，审批和失败外显，多条详情可同时展开；外层开合保持输出请求次数、内部开合和输出滚动位置；增量、完成不自动开合。
- 键盘 Enter 可控制外层，收起后的内部按钮不能获取焦点；视觉检查及无横向溢出检查通过。实际原生 details 使用内容可见性隐藏，验收采用 checkVisibility 而非仅依赖布局矩形。
- 最近历史分页、大输出加载与缓存重试、SSE 增量/恢复、发送/插话/草稿/导航浏览器回归通过；TypeScript 和生产构建通过。全量160项测试，159通过、1项依赖便携包路径的产物检查默认跳过。
- 独立审查及修正后的范围复查通过，无剩余发现。服务端接口未修改，前端资源已重新构建。
