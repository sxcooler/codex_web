# 第二阶段实施验收（2026-09-11）

本次保留原配色和消息样式，实施依据为 `docs/superpowers/specs/2026-09-11-phase2-design.md`。

## 已实现

- 三栏拖动、键盘调整与浏览器比例记忆；窄屏和折叠不覆盖宽屏偏好。
- 切换会话、打开新任务时自动请求释放；活动任务结束后释放，返回时取消待释放；手动释放收到确认才关闭页面。
- SSE 心跳超时与前后台恢复、原生重试状态、未知提交不自动重发；会话草稿、附件和阅读位置保留。
- 原生模型/推理强度与四种审批选项，下一轮生效；不可用能力禁用并说明原因。重命名、收藏、Web 隐藏和清除元数据。
- 安全文件浏览、按文件统一/并排 diff、patch 下载、明确关联命令的 JUnit 报告。
- 私有附件上传/预览、受控原生输入、配额及草稿清理；认证关联的 Web Push 和通知点击入口。

## 验证范围

自动测试使用真实临时文件、Git 仓库、SQLite、Fastify 认证，以及独立模拟 app-server；不消耗模型额度。浏览器测试使用隔离 Vite 页面与模拟 API，验证桌面拖动、刷新记忆、会话切换草稿、审批菜单、390×844 手机与 831px 布局、重连不重复发送、释放确认、快速返回期间刷新/聚焦、新任务项目上下文和后台创建自动释放。

截图位于本地 `output/playwright/desktop-permissions.png` 与 `mobile-chat.png`。后端单独 TypeScript 检查，前端执行 `npm.cmd run build`。最终结果：Node 26.2.0 与 Node 24.20.0 分别 121/121 测试通过；Edge 无头浏览器 5/5 场景通过；前端构建及后端 TypeScript 检查通过。

只读探测确认本机 `config/read` 的默认审批字段可能为 null。未显式选择时由原生 start/resume 返回有效权限，不猜测默认值；无法完整表达的显式自定义模式不可选。只读打开历史不能确认的模型/权限不冒充已生效配置。

历史命令缺少准确启动时间时，JUnit 报告保守提示可能过期。切走时尚未完成的上传暂停，保留文件供返回后重试；已经上传的附件保留。

## 尚未实际验收

第二阶段真实 VS Code ↔ Web 写入接力、真实手机后台 Push、通知点击后的设备登录/接力，均未执行。模拟与只读探测不代替这些验收。上一阶段真实接力记录仍单独保留。

本次只合入开发源代码，不重启承载当前对话的生产服务，不更新其运行中的构建，不修改全局 Codex 配置或原生历史。
